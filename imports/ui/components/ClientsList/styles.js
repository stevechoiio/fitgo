const styles = theme => ({
  rootClients: {
    flexGrow: 1,
    padding: theme.spacing.unit * 12,
    paddingTop: 0
  },
  heading: {
    width: '100%',
    textAlign: 'center'
  },
  control: {
    padding: theme.spacing.unit * 2
  }
});

export default styles;
